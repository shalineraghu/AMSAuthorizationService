apiVersion: v1
kind: Service
metadata:
  name: {{ template "authorizationservice.fullname" . }}
  labels:
    app: {{ template "authorizationservice.name" . }}
    chart: {{ template "authorizationservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authorizationservice.name" . }}
    release: {{ .Release.Name }}
